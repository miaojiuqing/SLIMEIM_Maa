name: install

env:
  # 版本控制：
  # - 设置 VERSION 为空字符串时，下载 latest 版本
  # - 设置 VERSION 为具体版本号时（如 v1.2.3），下载指定版本
  MAAFW_VERSION: ""
  MFAA_VERSION: ""
  # Android APK 在 MirrorChyan 上使用的 rid（桌面端仍然是 SLIMEIM_Maa，两者互不影响）：
  # 打包时会把 APK 内 interface.json 的 mirrorchyan_rid 改成它 —— MaaFwApp 的更新检查读的
  # 就是 PI 里的这个字段；正式版还会把 APK 上传到该 rid。留空时上传任务会自动跳过。
  ANDROID_RID: "SLIMEIM_Maa_exec"

on:
  push:
    tags:
      - "v*"
    branches:
      - "**"
    paths:
      - ".github/workflows/install.yml"
      - ".github/android/**"
      - "tools/ci/**"
      - "assets/**"
      - "agent/**"
      - "**.py"
      - "requirements.txt"
  pull_request:
    branches:
      - "**"
    paths:
      - ".github/workflows/install.yml"
      - ".github/android/**"
      - "tools/ci/**"
      - "assets/**"
      - "agent/**"
      - "**.py"
      - "requirements.txt"
  workflow_dispatch:

jobs:
  meta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: set_tag
        run: |
          # 检查标签格式
          is_release=${{ startsWith(github.ref, 'refs/tags/v') }}
          tag=$(git describe --tags --match "v*" ${{ github.ref }} || true)
          if [[ $tag != v* ]]; then
            tag=$(curl -sX GET "https://api.github.com/repos/${{ github.repository }}/releases/latest" --header 'authorization: Bearer ${{ secrets.GITHUB_TOKEN }}' | awk '/tag_name/{print $4}' FS='["]')
            if [[ $tag != v* ]]; then
              tag="v0.0.0"
            fi
            tag=$(date "+$tag-%y%m%d-$(git rev-parse --short HEAD)")
          fi

          if ! $($is_release) ; then
            prefix=${tag%-*-*}
            suffix=${tag#$prefix-}
            tag="$prefix-ci.$suffix"
          fi

          # 检查是否为预发布版本
          is_prerelease=false
          if [[ $tag =~ .*alpha.* || $tag =~ .*beta.* || $tag =~ .*rc.* || $tag =~ .*dev.* || $tag =~ .*-ci.* ]]; then
            is_prerelease=true
            echo "This is a pre-release version"
          fi

          echo tag=$tag | tee -a $GITHUB_OUTPUT
          echo is_release=$is_release | tee -a $GITHUB_OUTPUT
          echo is_prerelease=$is_prerelease | tee -a $GITHUB_OUTPUT
          echo android_rid=${{ env.ANDROID_RID }} | tee -a $GITHUB_OUTPUT
    outputs:
      tag: ${{ steps.set_tag.outputs.tag }}
      is_release: ${{ steps.set_tag.outputs.is_release }}
      is_prerelease: ${{ steps.set_tag.outputs.is_prerelease }}
      android_rid: ${{ steps.set_tag.outputs.android_rid }}

  install:
    needs: meta
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [win, macos, linux]
        arch: [aarch64, x86_64]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Download MaaFramework
        uses: robinraju/release-downloader@v1
        with:
          repository: MaaXYZ/MaaFramework
          fileName: "MAA-${{ matrix.os }}-${{ matrix.arch }}*"
          latest: ${{ env.MAAFW_VERSION == '' }}
          tag: ${{ env.MAAFW_VERSION }}
          out-file-path: "deps"
          extract: true

      - name: Download MFAAvalonia
        id: download_mfa
        uses: robinraju/release-downloader@v1
        with:
          repository: SweetSmellFox/MFAAvalonia
          fileName: "MFAAvalonia-*-${{ (matrix.os == 'win' && 'win') || (matrix.os == 'macos' && 'osx') || (matrix.os == 'linux' && 'linux') }}-${{ (matrix.arch == 'x86_64' && 'x64') || (matrix.arch == 'aarch64' && 'arm64') }}*"
          latest: ${{ env.MFAA_VERSION == '' }}
          tag: ${{ env.MFAA_VERSION }}
          out-file-path: "MFA"
          extract: true

      - name: Clean up MFAAvalonia archive
        shell: bash
        run: |
          ARCHIVE_FILE_PATH="${{ fromJson(steps.download_mfa.outputs.downloaded_files)[0] }}"
          rm -f "${ARCHIVE_FILE_PATH}"
          echo "Archive cleanup command executed for MFAAvalonia."

      - name: Remove built-in runtime in MFA
        shell: bash
        run: |
          rm -rf MFA/runtimes

      - name: Install
        shell: bash
        run: |
          if [ -d "MFA" ]; then
            echo "Copying MFA files to install directory..."
            mkdir -p install
            rsync -av --ignore-existing MFA/ install/
            echo "MFA files copied successfully."
          else
            echo "MFA directory not found, skipping copy."
          fi

          # 根 requirements.txt 提供 maa（tools/install.py 生成 resource.hash 用），
          # tools/requirements.txt 提供 json-with-comments（读写 interface.json 用）
          python -m pip install -r ./requirements.txt

          cd tools/
          python -m pip install -r ./requirements.txt
          python ./install.py ${{ needs.meta.outputs.tag }} ${{ matrix.os }} ${{ matrix.arch }}
          cd ..

      - uses: actions/upload-artifact@v4
        with:
          name: MaaSLIMEIM-${{ matrix.os }}-${{ matrix.arch }}
          path: "install"

  android:
    name: Build Android APK
    needs: [meta]
    runs-on: ubuntu-latest
    outputs:
      variant: ${{ steps.signing.outputs.variant }}
    env:
      # MaaFwApp 固定到一个已知可用的 commit，避免上游改动打挂发版
      MAAFWAPP_REF: f20ff4ba0b53e83132da1015c7635ef16e8b5736
      MAAFRAMEWORK_TAG: v5.12.3
      PI_PROFILE: ${{ github.workspace }}/.github/android/pi-profile.yaml
      ANDROID_RID: ${{ needs.meta.outputs.android_rid }}
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}

    steps:
      - name: Checkout SLIMEIM_Maa
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: Checkout MaaFwApp
        uses: actions/checkout@v4
        with:
          repository: Aliothmoon/MaaFwApp
          ref: ${{ env.MAAFWAPP_REF }}
          path: build-android/MaaFwApp
          fetch-depth: 1

      - name: Use SLIMEIM_Maa version for the APK
        shell: bash
        run: |
          set -euo pipefail
          # MaaFwApp 会从当前 Git 仓库生成 versionCode/versionName。移除嵌套 checkout
          # 的 Git 元数据后，它会向上使用本仓库的提交数与标签。
          rm -rf build-android/MaaFwApp/.git

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Configure Android package name
        shell: bash
        run: python .github/android/patch_maafwapp.py build-android/MaaFwApp

      - name: Prepare Project Interface assets
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install -r ./requirements.txt
          python -m pip install -r ./tools/requirements.txt
          python ./tools/configure.py

          pi_root="$GITHUB_WORKSPACE/build-android/pi-assets"
          rm -rf "$pi_root"
          mkdir -p "$pi_root"
          cp "$GITHUB_WORKSPACE/assets/interface.json" "$pi_root/interface.json"
          cp -a "$GITHUB_WORKSPACE/assets/resource" "$pi_root/resource"
          cp -a "$GITHUB_WORKSPACE/assets/tasks" "$pi_root/tasks"
          cp -a "$GITHUB_WORKSPACE/agent" "$pi_root/agent"
          cp "$GITHUB_WORKSPACE/assets/resource/image/logo.png" "$pi_root/logo.png"
          cp "$GITHUB_WORKSPACE/LICENSE" "$pi_root/LICENSE"

          python - <<'PY'
          from pathlib import Path
          import jsonc
          import os

          pi_root = Path(os.environ["GITHUB_WORKSPACE"]) / "build-android" / "pi-assets"
          interface_path = pi_root / "interface.json"
          with open(interface_path, encoding="utf-8") as handle:
              interface = jsonc.load(handle)

          tag = "${{ needs.meta.outputs.tag }}"
          interface["version"] = tag

          # MaaFwApp 的更新检查读的是 PI 里的 mirrorchyan_rid（并且请求固定带 os=android）。
          # 如果 Android 用的是独立 rid，必须在这里一起改掉，否则 app 会去查桌面那个 rid，
          # 拿不到 android 包（只影响 Android 包，桌面端 interface.json 不动）。
          rid = (os.environ.get("ANDROID_RID") or "").strip()
          if rid:
              interface["mirrorchyan_rid"] = rid
              print(f"Android mirrorchyan_rid -> {rid}")
          else:
              print("⚠️ 未配置 ANDROID_RID：APK 内仍是桌面 rid，MirrorChyan 上没有对应的 android 包")

          with open(interface_path, "w", encoding="utf-8") as handle:
              jsonc.dump(interface, handle, ensure_ascii=False, indent=4)
              handle.write("\n")
          PY

          python ./tools/ci/gen_resource_hash.py "$pi_root/interface.json" --root "$pi_root"

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Cache Android runtimes
        uses: actions/cache@v4
        with:
          path: |
            build-android/MaaFwApp/.maa-cache
            build-android/MaaFwApp/.maafw
          key: android-runtime-${{ runner.os }}-${{ env.MAAFRAMEWORK_TAG }}-${{ env.MAAFWAPP_REF }}

      - name: Prepare MaaFramework and Python agent
        working-directory: build-android/MaaFwApp
        shell: bash
        run: |
          set -euo pipefail
          python scripts/setup_maa_framework.py --tag "$MAAFRAMEWORK_TAG"
          python scripts/build_agent_bundle.py \
            --out "$GITHUB_WORKSPACE/build-android/agent-dist" \
            --abi arm64-v8a \
            --abi x86_64 \
            --requirements "$GITHUB_WORKSPACE/requirements.txt"

      - name: Configure Android signing
        id: signing
        shell: bash
        run: |
          set -euo pipefail
          values=(
            "$ANDROID_KEYSTORE_BASE64"
            "$ANDROID_KEYSTORE_PASSWORD"
            "$ANDROID_KEY_ALIAS"
            "$ANDROID_KEY_PASSWORD"
          )
          configured=0
          for value in "${values[@]}"; do
            [[ -n "$value" ]] && configured=$((configured + 1))
          done

          if [[ "$configured" -eq 0 ]]; then
            echo "No release keystore configured; building a debug-signed APK."
            echo "gradle_task=assembleDebug" >> "$GITHUB_OUTPUT"
            echo "variant=debug" >> "$GITHUB_OUTPUT"
            echo "suffix=-debug" >> "$GITHUB_OUTPUT"
          elif [[ "$configured" -ne 4 ]]; then
            echo "Android signing secrets are only partially configured." >&2
            echo "Set ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD, ANDROID_KEY_ALIAS and ANDROID_KEY_PASSWORD together." >&2
            exit 1
          else
            keystore="$RUNNER_TEMP/android-release.jks"
            printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "$keystore"
            test -s "$keystore"

            # 解码后立刻验一次：keystore 读不出来时，失败会晚到 :app:packageRelease 才爆，
            # 那里只抛 KeytoolException + EOFException，分不清是 base64 截断、二次编码还是密码/别名不对
            # secret 的值读不回来，所以只打长度：长度对不上就说明填错/截断了
            echo "keystore: $(stat -c%s "$keystore") bytes | base64: ${#ANDROID_KEYSTORE_BASE64} chars | sha256: $(sha256sum "$keystore" | cut -d' ' -f1)"
            echo "store password: ${#ANDROID_KEYSTORE_PASSWORD} chars | key password: ${#ANDROID_KEY_PASSWORD} chars | same: $([[ "$ANDROID_KEY_PASSWORD" == "$ANDROID_KEYSTORE_PASSWORD" ]] && echo yes || echo no)"

            if ! keytool -list -keystore "$keystore" -storepass "$ANDROID_KEYSTORE_PASSWORD" >/dev/null 2>&1; then
              echo "❌ 密钥库打不开：ANDROID_KEYSTORE_PASSWORD 与 keystore 不匹配（长度见上一行）" >&2
              exit 1
            fi

            aliases="$(keytool -list -keystore "$keystore" -storepass "$ANDROID_KEYSTORE_PASSWORD" 2>/dev/null | awk -F',' '/PrivateKeyEntry/ {print $1}' | tr '\n' ' ')"
            if ! keytool -list -keystore "$keystore" -storepass "$ANDROID_KEYSTORE_PASSWORD" -alias "$ANDROID_KEY_ALIAS" >/dev/null 2>&1; then
              echo "❌ 密钥库能打开，但没有 ANDROID_KEY_ALIAS 指定的那个别名；keystore 里实际是：${aliases:-<无>}" >&2
              exit 1
            fi
            echo "keystore aliases: ${aliases:-<无>}"

            echo "KEYSTORE_PATH=$keystore" >> "$GITHUB_ENV"
            echo "KEYSTORE_PASSWORD=$ANDROID_KEYSTORE_PASSWORD" >> "$GITHUB_ENV"
            echo "KEY_ALIAS=$ANDROID_KEY_ALIAS" >> "$GITHUB_ENV"
            echo "KEY_PASSWORD=$ANDROID_KEY_PASSWORD" >> "$GITHUB_ENV"
            echo "gradle_task=assembleRelease" >> "$GITHUB_OUTPUT"
            echo "variant=release" >> "$GITHUB_OUTPUT"
            echo "suffix=" >> "$GITHUB_OUTPUT"
          fi

      - name: Build APK
        working-directory: build-android/MaaFwApp
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          ./gradlew ":app:${{ steps.signing.outputs.gradle_task }}" --stacktrace

          variant="${{ steps.signing.outputs.variant }}"
          apk="$(find "app/build/outputs/apk/$variant" -maxdepth 1 -type f -name '*.apk' -print -quit)"
          test -n "$apk"
          mkdir -p "$GITHUB_WORKSPACE/android-dist"
          cp "$apk" "$GITHUB_WORKSPACE/android-dist/MaaSLIMEIM-android-${{ needs.meta.outputs.tag }}${{ steps.signing.outputs.suffix }}.apk"

      - uses: actions/upload-artifact@v4
        with:
          name: MaaSLIMEIM-android
          path: android-dist/*.apk
          if-no-files-found: error

  changelog:
    name: Generate changelog
    needs: meta
    runs-on: ubuntu-latest
    outputs:
      release_body: ${{ steps.git-cliff.outputs.content }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate a changelog
        uses: orhun/git-cliff-action@v4
        id: git-cliff
        with:
          config: .github/cliff.toml
          # 正式版：使用 tag-pattern 只匹配正式版标签，包含所有从上一个正式版以来的提交
          # 预发布版：正常生成，只包含从上一个标签以来的提交
          args: >-
            -vv --latest --strip header
            ${{ needs.meta.outputs.is_prerelease == 'false' && '--tag-pattern "^v[0-9]+\\.[0-9]+\\.[0-9]+$"' || '' }}
        env:
          OUTPUT: CHANGES.md
          GITHUB_REPO: ${{ github.repository }}

  release:
    if: ${{ needs.meta.outputs.is_release == 'true' }}
    needs: [meta, install, android, changelog]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: assets

      - run: |
          set -euo pipefail
          cd assets
          tag="${{ needs.meta.outputs.tag }}"
          for f in *; do
            [[ -d "$f" ]] || continue
            if [[ "$f" == *-android* ]]; then
              # APK 直接进 Release（不再套一层 zip）
              found=0
              for apk in "$f"/*.apk; do
                [[ -f "$apk" ]] || continue
                mv "$apk" .
                found=1
              done
              if [[ "$found" -eq 0 ]]; then
                echo "missing APK in $f"
                ls -la "$f"
                exit 1
              fi
              rm -rf "$f"
            else
              (cd "$f" && zip -r "../$f-${tag}.zip" .)
            fi
          done
          ls -la

      - uses: softprops/action-gh-release@v2
        with:
          files: |
            assets/*.zip
            assets/*.apk
          tag_name: ${{ needs.meta.outputs.tag }}
          body: ${{ needs.changelog.outputs.release_body }}
          draft: false
          prerelease: ${{ needs.meta.outputs.is_prerelease == 'true' }}

      - name: Trigger MirrorChyanUploading
        shell: bash
        run: |
          gh workflow run --repo $GITHUB_REPOSITORY mirrorchyan_release.yml -f tag=${{ needs.meta.outputs.tag }}
          gh workflow run --repo $GITHUB_REPOSITORY mirrorchyan_release_note.yml -f tag=${{ needs.meta.outputs.tag }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  android_mirrorchyan:
    name: Upload Android APK to MirrorChyan
    # 没配 ANDROID_RID 时不传（上传一定会失败），只会影响这一个 job
    if: ${{ needs.meta.outputs.is_release == 'true' && needs.android.outputs.variant == 'release' && needs.meta.outputs.android_rid != '' }}
    needs: [meta, android, release]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: MaaSLIMEIM-android
          path: android-dist

      - name: Locate Android APK
        id: apk
        shell: bash
        run: |
          set -euo pipefail
          apk="$(find android-dist -type f -name '*.apk' -print -quit)"
          test -n "$apk"
          echo "path=$apk" >> "$GITHUB_OUTPUT"

      - name: Upload Android APK
        uses: MirrorChyan/uploading-action@v1
        with:
          filetype: local
          filename: ${{ steps.apk.outputs.path }}
          mirrorchyan_rid: ${{ needs.meta.outputs.android_rid }}
          version_name: ${{ needs.meta.outputs.tag }}
          os: android

          owner: miaojiuqing
          repo: SLIMEIM_Maa
          github_token: ${{ secrets.GITHUB_TOKEN }}
          upload_token: ${{ secrets.ANDROID_MIRRORCHYANUPLOADTOKEN }}
